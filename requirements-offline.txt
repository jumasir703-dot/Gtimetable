# Requirements for building the offline desktop version with PyInstaller.
# psycopg2-binary is deliberately left out: offline mode always uses the
# built-in SQLite fallback in database.py, so Postgres isn't needed.
Flask==3.0.3
reportlab==4.2.5
pyinstaller==6.10.0
pywebview==6.2.1
